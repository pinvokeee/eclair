import { expect, it } from 'vitest';
// import { Formula } from '../core/formula';

it("aaa", () => {

    // const f = new Formula();    

    // const ab = f.eval("/*tesdwadawdawdwadawat*/ 3 + 4 * 2 / ( 1 - 5 ) ^ 2 ^ 3");
    // const cd = f.eval("3 + 4 * 2 / ( 1 - 5 ) ^ [ab] ^ 3");

    // // const cd = f.eval("1+20+3/4*05.001+(6*800)+[ab]*(70+9)").join(" ");

    // console.log(ab);
    // console.log(cd);

})